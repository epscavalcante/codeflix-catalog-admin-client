version: '3'

services:

  codeflix-catalog-admin-frontend:
    build: .
    tty: true
    container_name: codeflix-catalog-admin-frontend
    # entrypoint: ./entrypoint.sh
    ports:
      - "5147:5147"
    volumes:
      - ..:/home/node/app